<UserControl x:Class="tamagotchi.Views.ChoosePetVariantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tamagotchi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--<StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Background="White">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Padding="20" Margin="20, 10"  BorderThickness="1" BorderBrush="Gray" CornerRadius="20">
                        <Image Source="/Assets/Images/Dogs/Dog_Brown.png" Width="50" Stretch="UniformToFill"  />
                    </Border>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="BackButton" Width="300" Margin="10, 10" Background="LightBlue" Click="BackButton_Click">BACK</Button>
                    <Button x:Name="StartButton" Width="300" Margin="10, 10" Background="LightBlue" Click="StartButton_Click">Start Game</Button>

                </StackPanel>
            </StackPanel>-->

            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Background="White">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Padding="20" Margin="20, 10" BorderThickness="1" BorderBrush="Gray" CornerRadius="20">
                        <Image x:Name="PetImage" Width="50" Stretch="UniformToFill" />
                    </Border>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Pet name:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="PetNameInput" Width="200" Margin="10, 10" Padding="5"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="BackButton" Width="300" Margin="10, 10" Background="LightBlue" Click="BackButton_Click">BACK</Button>
                    <Button x:Name="StartButton" Width="300" Margin="10, 10" Background="LightBlue" Click="StartButton_Click">Start Game</Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
